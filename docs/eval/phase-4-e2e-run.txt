Phase 4 - Playwright suite, verbatim run output (auth + career + generate + scan).

This file exists because SPEC Block F states that manual live verification is
not accepted as evidence - the spec run is. A spec FILE is cron.job; this is
cron.job_run_details.

Command:      npx playwright test
Date:         2026-09-03 UTC
Branch:       phase-4-generate
Commit:       (this run) fix: close the architect pass on the Phase 4 diff
Working tree: CLEAN for src/ and tests/ - the commit above is exactly the code
              that was tested. Only this file and the docs beside it are
              committed after it, and none of them is loaded at runtime.

THIS RUN REPLACES an earlier one recorded against "test(e2e): the US-4/US-5 path
against the Hiredbuddy case". It was re-run because the architect's diff review
changed things a model can SEE: the vacancy keyword list now goes to P3 and not
to P2, `MAX_TOKENS_BY_STEP.judge` went 1200 -> 3000, and the re-score's chunker
stopped borrowing a storage cap. A re-run was the only way to know what those did
- the argument that they are improvements is cheap to make and easy to get wrong,
and a run is neither.
Against:      npm run dev on http://localhost:3000, started and torn down by
              Playwright itself (real Supabase project, EU-Frankfurt) and real
              OpenRouter calls.

WHAT THE PHASE-4 CASES RUN AGAINST. `docs/eval/calibration-case-hiredbuddy.json`
- the owner's own reconstruction of the posting the coverage thresholds were
calibrated against. A senior AI-quality career base scanned against an
entry-level annotation role, so the generator is asked for material the base
genuinely does not have: the base names none of MS Office, Google Suite,
Labelbox or Supervisely, all four of which the posting asks for. A vacancy the
base answered perfectly would never exercise what this phase is about.

WHAT THIS SUITE CANNOT SEE, stated so nobody reads more into it than it says.
It does not query `llm_calls` or `resume_versions`: `.from(` outside a DAL fails
check.mjs R1 and `tests/` is in scope, so "exactly two chat rows for an approved
run" is not observable from here. What IS observable is the number of versions a
run wrote (below), the request count for a double-clicked button, and every
verdict the judge returned. The row count is the owner query in the hand-over.


===========================================================================
RE-RUN AFTER THE OWNER-TESTING ROUND (SPEC v2.17)
===========================================================================

Command:      npx playwright test
Date:         2026-09-04 UTC
Commit:       (this run) fix: close the architect pass on the owner-testing round
Result:       31 passed, 2 skipped.

RE-RUN ONCE MORE after the architect's findings were fixed - the prompt slots
moved into a <candidate_name> data block, the display name sanitised, the judge
panel's corpus unified across all three sites, and the e2e probe narrowed. Same
result, and it is named here rather than argued about: the fixes touched two
prompts and the corpus a rendered panel is computed from, and a re-run is the
only thing that says what they did.

The two skips are both VISIBLE and both say why:

  -  23  generate > a saved display name becomes the resume name line and the
         file name
         SKIPPED: needs migration 004_profiles.sql applied in the Supabase
         dashboard. It probes by performing the feature's own first save, so it
         cannot drift from what it guards, and it starts running by itself the
         moment the table exists. Until the owner applies 004, THE DISPLAY-NAME
         FEATURE IS UNWITNESSED end to end (backlog p4-19).
  -  33  scan > an AI failure saves the vacancy as a draft and says so
         The documented pre-existing skip: it needs a server started with a
         deliberately invalid OPENROUTER_API_KEY.

Its own output, verbatim:

  ok  1 [chromium] > tests>e2e>auth.spec.ts:122:3 > auth > sign-up lands on /career (2.2s)
  ok  2 [chromium] > tests>e2e>auth.spec.ts:129:3 > auth > sign-in lands on /scan, sign-out returns to /login (2.9s)
  ok  3 [chromium] > tests>e2e>auth.spec.ts:142:3 > auth > a signed-in user is bounced off /login and /signup (2.5s)
  ok  4 [chromium] > tests>e2e>auth.spec.ts:150:3 > auth > a visitor is redirected off member routes with no data flash (1.0s)
  ok  5 [chromium] > tests>e2e>auth.spec.ts:163:3 > auth > /privacy is public and reachable from both layouts (2.4s)
  ok  6 [chromium] > tests>e2e>auth.spec.ts:178:3 > auth > wrong password shows the exact copy and no other outcome (3.3s)
  ok  7 [chromium] > tests>e2e>auth.spec.ts:207:5 > auth > client-side validation blocks submit on a malformed email (495ms)
  ok  8 [chromium] > tests>e2e>auth.spec.ts:207:5 > auth > client-side validation blocks submit on a too-short password (475ms)
  ok  9 [chromium] > tests>e2e>auth.spec.ts:225:3 > auth > native validation never pre-empts our copy (440ms)
  ok 10 [chromium] > tests>e2e>auth.spec.ts:241:3 > auth > a duplicate email is reported as such (3.1s)
  ok 11 [chromium] > tests>e2e>auth.spec.ts:252:3 > auth > the session cookie is httpOnly, SameSite=Lax and capped at 30 days (1.7s)
  ok 12 [chromium] > tests>e2e>auth.spec.ts:296:3 > auth > account deletion removes the user and returns to /login with the toast (12.6s)
  ok 13 [chromium] > tests>e2e>career.spec.ts:228:3 > career base > every career endpoint refuses a signed-out caller with 401 (1.1s)
  ok 14 [chromium] > tests>e2e>career.spec.ts:254:3 > career base > a new account sees the exact empty state (2.0s)
  ok 15 [chromium] > tests>e2e>career.spec.ts:260:3 > career base > a scanned PDF shows the exact 422 copy, pre-opens the paste tab, and saves nothing (3.2s)
  ok 16 [chromium] > tests>e2e>career.spec.ts:296:3 > career base > a text-layer PDF imports, reviews and saves, and a second import ADDS (9.8s)
  ok 17 [chromium] > tests>e2e>career.spec.ts:329:3 > career base > re-importing the SAME text saves nothing and reports every skip (9.1s)
  ok 18 [chromium] > tests>e2e>career.spec.ts:354:3 > career base > a metered Save spends once even when double-clicked (8.3s)
  ok 19 [chromium] > tests>e2e>generate.spec.ts:159:3 > generate > all four Phase-4 endpoints refuse a signed-out caller with 401 (3.1s)
  ok 20 [chromium] > tests>e2e>generate.spec.ts:181:3 > generate > another user's application is 404 on every Phase-4 endpoint, never 403 (2.0s)
  ok 21 [chromium] > tests>e2e>generate.spec.ts:197:3 > generate > generate, judge, edit, re-score, export — the whole US-4/US-5 path (53.9s)
  ok 22 [chromium] > tests>e2e>generate.spec.ts:522:3 > generate > one click, one spend: a double-clicked [Generate] sends ONE request (39.8s)
  -  23 [chromium] > tests>e2e>generate.spec.ts:568:3 > generate > a saved display name becomes the resume name line and the file name
  ok 24 [chromium] > tests>e2e>generate.spec.ts:625:3 > generate > an empty editor blocks both metered buttons with the exact copy (11.6s)
  ok 25 [chromium] > tests>e2e>generate.spec.ts:649:3 > generate > the judge catches invention, and rule B2 forces revise however good the rest is (22.9s)
  ok 26 [chromium] > tests>e2e>generate.spec.ts:734:3 > generate > [Add to resume] is refused with a reason while no resume exists (12.1s)
  ok 27 [chromium] > tests>e2e>scan.spec.ts:214:3 > scan > both scan endpoints refuse a signed-out caller with 401 (812ms)
  ok 28 [chromium] > tests>e2e>scan.spec.ts:244:3 > scan > an oversized vacancy is refused with the exact copy, before any spend (2.7s)
  ok 29 [chromium] > tests>e2e>scan.spec.ts:275:3 > scan > an empty career base blocks the Career-base source with the exact notice (3.1s)
  ok 30 [chromium] > tests>e2e>scan.spec.ts:288:3 > scan > the applications list starts empty with the exact copy (2.6s)
  ok 31 [chromium] > tests>e2e>scan.spec.ts:294:3 > scan > a pasted resume scans, scores, and renders coverage, keywords and base matches (14.1s)
  ok 32 [chromium] > tests>e2e>scan.spec.ts:461:3 > scan > a career-base scan says the base is the source, instead of an empty tab (19.6s)
  -  33 [chromium] > tests>e2e>scan.spec.ts:503:3 > scan > an AI failure saves the vacancy as a draft and says so

  2 skipped
  31 passed (4.4m)

What the phase-4 diagnostics said on this run:

  [phase-4] run: {"versions":2,"autoRevised":true,"revisionNotBetter":false,
                  "revisionWithheld":false,
                  "verdicts":[{"source":"ai","verdict":"revise","grounding":"fail","violations":3},
                              {"source":"ai_revision","verdict":"revise","grounding":"fail","violations":3}]}
  [phase-4] header: {"literalNamePlaceholder":false,"namePlaceholder":true}
  [phase-4] rescore: {"stored":"57%","live":"56%","entries":8}
  [phase-4] grounding gate: {"verdict":"revise","grounding":"fail","violations":5,
                             "scores":{"keywordCoverage":3,"relevance":1,"atsFormat":4}}

  * `namePlaceholder: true` is the v2.17 fix observed working. No display name is
    saved on this path, so the resume's name line is the VISIBLE `[YOUR NAME]`
    placeholder — not the vacancy's job title, which is what owner testing found
    there, and not a silent substitution. The test now asserts it rather than
    logging a boolean that could not fail: it checks the first line is not
    "Data Annotator", that the draft contains the placeholder, that the editor
    says so, that the filename is not `CV_Data_Annotator_…`, and that the
    download raises the warning. `literalNamePlaceholder` (P2's own layout token
    `NAME`) stays false and is now measured by a regex that can actually match.
  * The judge panel's base gate is asserted ON THE RENDERED SCREEN: nothing under
    "Supported by your base, missing from the resume" may name Labelbox,
    Supervisely, MS Office or Google Suite, none of which this fixture's base
    contains. That is the owner's own defect, and it now fails a test rather than
    a reading.
  * `rescore: 57% -> 56%` — the live number moved DOWN this time, which is worth
    recording beside the earlier 65% and 62%: the delta is not a quality signal
    and is not attributable to the edit alone (see the corpus note above and
    backlog p4-11).

A NOTE ON TWO EARLIER RUNS OF THIS SUITE THAT FAILED, and why they are not in
this file as failures. Both were run against a dev server left ORPHANED by
stopping an earlier Playwright run mid-flight: `reuseExistingServer` picked the
broken process back up, and it was by then logging Turbopack worker crashes and
`write EPIPE`. Twelve auth and career tests failed on routes that touch nothing
this round changed, `/generate` answered 500 where it answers 401, and the whole
US-4/US-5 path took 10.4 minutes instead of 54 seconds. Killing that server by
its PID and re-running gave the result above with no code change. Recorded
because the first reading of those failures was "a regression in this branch",
and it was not - it was the test harness, and the way to tell was to reproduce
one failure by hand rather than to read the list.

---------------------------------------------------------------------------
RESULT - the FIRST run, against SPEC v2.16 (superseded, kept for the record)
---------------------------------------------------------------------------

This block and the "WHAT THE PHASE-4 RUNS ACTUALLY DID" section under it are the
v2.16 run. They are kept because they carry the two DoD branches (an approved
first draft, and a revision) and the defect this suite found in its own branch.
The run that ships is the one above.

  ok  1 [chromium] > tests\e2e\auth.spec.ts:122:3 > auth > sign-up lands on /career (2.3s)
  ok  2 [chromium] > tests\e2e\auth.spec.ts:129:3 > auth > sign-in lands on /scan, sign-out returns to /login (2.9s)
  ok  3 [chromium] > tests\e2e\auth.spec.ts:142:3 > auth > a signed-in user is bounced off /login and /signup (2.3s)
  ok  4 [chromium] > tests\e2e\auth.spec.ts:150:3 > auth > a visitor is redirected off member routes with no data flash (1.0s)
  ok  5 [chromium] > tests\e2e\auth.spec.ts:163:3 > auth > /privacy is public and reachable from both layouts (2.2s)
  ok  6 [chromium] > tests\e2e\auth.spec.ts:178:3 > auth > wrong password shows the exact copy and no other outcome (3.3s)
  ok  7 [chromium] > tests\e2e\auth.spec.ts:207:5 > auth > client-side validation blocks submit on a malformed email (490ms)
  ok  8 [chromium] > tests\e2e\auth.spec.ts:207:5 > auth > client-side validation blocks submit on a too-short password (507ms)
  ok  9 [chromium] > tests\e2e\auth.spec.ts:225:3 > auth > native validation never pre-empts our copy (456ms)
  ok 10 [chromium] > tests\e2e\auth.spec.ts:241:3 > auth > a duplicate email is reported as such (3.1s)
  ok 11 [chromium] > tests\e2e\auth.spec.ts:252:3 > auth > the session cookie is httpOnly, SameSite=Lax and capped at 30 days (1.6s)
  ok 12 [chromium] > tests\e2e\auth.spec.ts:296:3 > auth > account deletion removes the user and returns to /login with the toast (12.5s)
  ok 13 [chromium] > tests\e2e\career.spec.ts:228:3 > career base > every career endpoint refuses a signed-out caller with 401 (1.0s)
  ok 14 [chromium] > tests\e2e\career.spec.ts:254:3 > career base > a new account sees the exact empty state (2.3s)
  ok 15 [chromium] > tests\e2e\career.spec.ts:260:3 > career base > a scanned PDF shows the exact 422 copy, pre-opens the paste tab, and saves nothing (2.6s)
  ok 16 [chromium] > tests\e2e\career.spec.ts:296:3 > career base > a text-layer PDF imports, reviews and saves, and a second import ADDS (11.3s)
  ok 17 [chromium] > tests\e2e\career.spec.ts:329:3 > career base > re-importing the SAME text saves nothing and reports every skip (8.6s)
  ok 18 [chromium] > tests\e2e\career.spec.ts:354:3 > career base > a metered Save spends once even when double-clicked (8.8s)
  ok 19 [chromium] > tests\e2e\generate.spec.ts:156:3 > generate > all four Phase-4 endpoints refuse a signed-out caller with 401 (3.1s)
  ok 20 [chromium] > tests\e2e\generate.spec.ts:178:3 > generate > another user's application is 404 on every Phase-4 endpoint, never 403 (2.1s)
  ok 21 [chromium] > tests\e2e\generate.spec.ts:194:3 > generate > generate, judge, edit, re-score, export - the whole US-4/US-5 path (38.7s)
  ok 22 [chromium] > tests\e2e\generate.spec.ts:451:3 > generate > one click, one spend: a double-clicked [Generate] sends ONE request (37.9s)
  ok 23 [chromium] > tests\e2e\generate.spec.ts:483:3 > generate > an empty editor blocks both metered buttons with the exact copy (14.8s)
  ok 24 [chromium] > tests\e2e\generate.spec.ts:507:3 > generate > the judge catches invention, and rule B2 forces revise however good the rest is (25.6s)
  ok 25 [chromium] > tests\e2e\generate.spec.ts:592:3 > generate > [Add to resume] is refused with a reason while no resume exists (13.4s)
  ok 26 [chromium] > tests\e2e\scan.spec.ts:214:3 > scan > both scan endpoints refuse a signed-out caller with 401 (1.4s)
  ok 27 [chromium] > tests\e2e\scan.spec.ts:244:3 > scan > an oversized vacancy is refused with the exact copy, before any spend (1.8s)
  ok 28 [chromium] > tests\e2e\scan.spec.ts:275:3 > scan > an empty career base blocks the Career-base source with the exact notice (2.2s)
  ok 29 [chromium] > tests\e2e\scan.spec.ts:288:3 > scan > the applications list starts empty with the exact copy (2.7s)
  ok 30 [chromium] > tests\e2e\scan.spec.ts:294:3 > scan > a pasted resume scans, scores, and renders coverage, keywords and base matches (13.2s)
  ok 31 [chromium] > tests\e2e\scan.spec.ts:461:3 > scan > a career-base scan says the base is the source, instead of an empty tab (11.8s)
  -  32 [chromium] > tests\e2e\scan.spec.ts:503:3 > scan > an AI failure saves the vacancy as a draft and says so

  1 skipped
  31 passed (4.0m)

THE SKIP IS THE DOCUMENTED ONE and is unchanged from Phase 3: the AI-unavailable
case needs a server started with a deliberately invalid OPENROUTER_API_KEY,
because the model call is server-side and no browser script can make it fail.
It is skipped VISIBLY rather than passing quietly - a test that passes when it
cannot run is worse than no test. Its docblock carries the two commands.

---------------------------------------------------------------------------
WHAT THE PHASE-4 RUNS ACTUALLY DID
---------------------------------------------------------------------------

The spec prints one metadata line per metered run - verdicts, scores and counts
only, never the resume text and never a violated claim, both of which are
personal data and neither of which belongs in a log (CLAUDE.md, Privacy).

From the v2.16 run (NOT the re-run above — its numbers are under its own
heading):

  [phase-4] run: {"versions":2,"autoRevised":true,"revisionNotBetter":false,
                  "revisionWithheld":false,
                  "verdicts":[{"source":"ai","verdict":"revise","grounding":"fail","violations":3},
                              {"source":"ai_revision","verdict":"revise","grounding":"fail","violations":2}]}
  [phase-4] header: {"literalNamePlaceholder":false}
  [phase-4] rescore: {"stored":"57%","live":"62%","entries":8}
  [phase-4] grounding gate: {"verdict":"revise","grounding":"fail","violations":5,
                             "scores":{"keywordCoverage":3,"relevance":1,"atsFormat":4}}

Read against the declared budget:

  * TWO versions, not three. The first draft was judged `revise` on grounding,
    rule B3's single revision ran with the reviewer's three specific violations
    in hand, the rewrite was judged again and still came back `revise` - with
    TWO violations rather than three, so the rewrite improved the draft without
    clearing it. The run stopped there and returned the better of the two with
    its honest card, which is Block D #5's own instruction. A loop would have
    shown up here as a third version and did not.
  * FOUR chat calls for that run (generate, judge, generate, judge) - the middle
    figure of the declared 2 / 4 / 8. NO repair retry was logged, unlike the
    pre-fix run; `MAX_TOKENS_BY_STEP.judge` at 1200 was the likely cause of that
    one, and it is 3000 now (backlog p4-2).
  * `revisionNotBetter: false` - `bestVersion` chose the REVISION this time,
    because it carried fewer violations. The earlier run went the other way and
    kept the first draft. Both rows exist either way, and the card says which one
    the reader is looking at.
  * The re-score moved 57% -> 62% on the SAME vacancy after two lines were added
    to the editor, with no chat call.

    THE DELTA IS NOT ATTRIBUTABLE TO THE EDIT, and saying so is the point of
    writing it down. The two numbers measure different corpora: the stored one
    ranks the requirements against the CAREER BASE through match_documents over
    the whole index, the live one against the ~20 ephemeral units of a single
    resume. Rule B1's thresholds were calibrated in coverage-thresholds.md
    against the first corpus only. So some of the 8 points is the edit and some
    of it is the change of corpus, and this run recorded no re-score of the
    UNEDITED draft that would separate them. What the run does establish is that
    the endpoint measures the editor's text - every re-scored entry names no
    career item, and the similarities are not the stored ones re-served, both of
    which the spec asserts. Calibrating the second corpus is backlog p4-11.

    Across four runs of this case the live number came out 57%, 58%, 65% and 62%
    against the same stored 57% - including one that matched it exactly, two real
    measurements of different text rounding to the same integer. That is why the
    spec does not assert inequality: it would have failed on a day the app was
    right.

From an EARLIER run of the same spec, on the same fixture, recorded because it
is the other DoD branch and a model decides which one happens:

  [phase-4] run: {"versions":1,"autoRevised":false,"revisionNotBetter":false,
                  "revisionWithheld":false,
                  "verdicts":[{"source":"ai","verdict":"approve","grounding":"pass","violations":0}]}

  * ONE version, TWO chat calls, no revision - the judge approved the first
    draft. "One revision when the rubric fails, none when it passes" is
    therefore witnessed in both directions, across two runs, against the same
    posting and the same career base.

THE GROUNDING GATE, END TO END. The `judge` case submits a resume claiming a
Senior Staff Engineer role at Google, a team of 40, a PhD from Stanford and an
AWS certification, against a base of two annotation jobs at fictional companies.
Five violations, `grounding: fail`, verdict `revise` - and that verdict is
COMPUTED by lib/judge.ts from the report's own evidence, not read from the
model's `verdict` field. atsFormat scored 4 in the same report, which is the
point of rule B2 being a gate: a well-formatted invention is still an invention.

A DEFECT THIS SUITE FOUND IN ITS OWN BRANCH, before any reviewer did. The first
run of the whole path failed on the assertion that the draft must not name
Labelbox or Supervisely. It named both, in a SKILLS line reading "ms office,
google suite, labelbox, supervisely" - the vacancy's keyword list, pasted whole
into a resume grounded in a base that contains none of it. The cause was in this
branch's prompt assembly, not in the model alone: `parsedRequirementsJson`
carried `parsed.keywords`, and a tidy list of skill terms beside an instruction
to use the vacancy's exact spelling is an invitation to reproduce it. Block F's
template asks P2 for "Vacancy requirements"; the keyword list is out of the
WRITER'S prompt and stays in the REVIEWER'S, which is the party rule B4 is
enforced by (SPEC v2.16, endpoint #5 note 13). The assertion is permanent and has
passed on every run since.

ONE MORE OBSERVATION - AND THE CHECK THAT WAS MEANT TO TRACK IT WAS BROKEN.
That same first failing run emitted the literal token `NAME` as the resume's
first line - P2 rule 4's layout placeholder rather than a name - which would then
have become the export filename. This file previously reported that it "has not
recurred", on the strength of a boolean the spec logged on every run. That
boolean was worthless: the regex behind it was written into the file as
`/^NAME<BS>/`, carrying a literal backspace byte where a `` word boundary was
intended, so it could never match anything and reported `false` on every run
regardless of what the resume said. The corruption came from a heredoc during
authoring, it is fixed, and a sweep of the whole branch found no other file
carrying a stray control byte.

So the honest statement is that the placeholder was seen ONCE and was never
afterwards MEASURED. The cause is real either way: the career base is a list of
jobs and skills and carries no name anywhere, so a grounded generator has nothing
to put on that line. Refusing to invent one is correct behaviour; the fix is to
ASK for a name, and SPEC v2.17 does - a `profiles` row, a Settings field, and a
visible `[YOUR NAME]` placeholder when it is unset. The e2e now asserts the
placeholder's PRESENCE on the no-name path and its ABSENCE on the named one,
which is a check that can fail.

===========================================================================
RE-RUN AFTER THE PR REVIEW ROUND (SPEC v2.18) - MIGRATION 004 APPLIED
===========================================================================

Command:      npx playwright test
Date:         2026-09-04 UTC
Commit:       (this run) the four PR-review fixes on phase-4-generate - the
              CLAUDE.md amendment, the total max_tokens map, rule B7a, and the
              e2e probe below. This file is committed after them, as the
              header of the first run above says of itself.
Result:       32 passed, 1 skipped. Exit code 0.

WHY THIS RUN EXISTS. `docs/reviews/phase-4.md` raised it as a BLOCKER: the run
recorded above skipped the display-name feature for want of migration 004, and
commit 60221b6 ("004 matches what was applied") establishes that the owner has
since applied it. The skip's stated reason had therefore lapsed while /privacy
already carried the public promise that the name is stored, used for two things,
and deleted with the account - and CLAUDE.md's Process rule is that a
user-facing promise may not ship ahead of the evidence.

WHAT THE FIRST ATTEMPT FOUND, and it was not the feature. With 004 applied the
suite came back 30 passed, 2 FAILED, 1 skipped, and both failures were the same
probe rather than the same feature:

  x  23  generate > a saved display name becomes the resume name line and the
         file name
  x  12  auth > account deletion removes the user and returns to /login with
         the toast

Both stopped on `expect(displayNameFailed).toBeVisible()` - the assertion that
guards the skip - and Playwright's own error context settles what was on screen
at that moment:

    - textbox "Your name":
      - text: Mira Steinberg
    - button "Save name"
    - status: Name saved.

The save had SUCCEEDED. The probe read the outcome with
`locator.isVisible({ timeout: 10_000 })`, and `isVisible()` does not auto-wait -
Playwright ignores a timeout handed to it - so it returned false the instant the
click dispatched, before the Server Action had answered, and the test then
demanded the FAILURE copy of a save that was about to succeed. That probe could
only ever pass as a SKIP, on the one path where the failure copy renders
immediately. Applying 004 turned it red for exactly the reason it should have
turned green: the feature started working and the probe could not see it.

So the blocker's real finding is about the TEST, and it is the finding a
never-executed guard always eventually produces. `.or()` now waits for whichever
outcome arrives, the skip still fires only on the migration-explained failure,
and any other outcome - a hang, a crash, a silently-wrong success - fails there
instead of disappearing. Nothing in `src/` was changed to make these two pass;
the diff for this half is two test files.

WHAT IS NOW WITNESSED, for the first time end to end:
  - The display-name path (case 23, 39.1s): saved in Settings, still there after
    a reload, on the generated resume's NAME line, absent as a placeholder, and
    in the download filename as `Mira_Steinberg` - then cleared back to empty,
    because an optional field a user cannot empty is one they cannot take back.
  - The `profiles` CASCADE (case 12, 13.7s). This is the half the report called
    out and the half the first attempt never reached: it failed four lines before
    the delete. The case now writes a profile row and THEN deletes the account,
    so CLAUDE.md's "removes the auth user and all owned rows (verified by test)"
    covers eight tables rather than seven.

THE ONE REMAINING SKIP is the documented pre-existing one, unchanged:

  -  33  scan > an AI failure saves the vacancy as a draft and says so
         Needs a server started with a deliberately invalid OPENROUTER_API_KEY,
         which no browser script can arrange from inside a running suite.

A NOTE ON THE EXPECTED NUMBER. Both the review report and the instruction that
commissioned this run predicted "33 passed, 1 skipped". The suite contains 33
cases in total and the previous run was 31 passed + 2 skipped, so un-skipping one
of them yields 32 + 1, not 33 + 1. The prediction double-counted the case it was
about. Recorded because the number in a spec run is the thing nobody re-derives.

Its own output, verbatim:

    ok  1 [chromium] › tests\e2e\auth.spec.ts:130:3 › auth › sign-up lands on /career (2.5s)
    ok  2 [chromium] › tests\e2e\auth.spec.ts:137:3 › auth › sign-in lands on /scan, sign-out returns to /login (2.9s)
    ok  3 [chromium] › tests\e2e\auth.spec.ts:150:3 › auth › a signed-in user is bounced off /login and /signup (2.4s)
    ok  4 [chromium] › tests\e2e\auth.spec.ts:158:3 › auth › a visitor is redirected off member routes with no data flash (1.1s)
    ok  5 [chromium] › tests\e2e\auth.spec.ts:171:3 › auth › /privacy is public and reachable from both layouts (2.3s)
    ok  6 [chromium] › tests\e2e\auth.spec.ts:186:3 › auth › wrong password shows the exact copy and no other outcome (3.2s)
    ok  7 [chromium] › tests\e2e\auth.spec.ts:215:5 › auth › client-side validation blocks submit on a malformed email (485ms)
    ok  8 [chromium] › tests\e2e\auth.spec.ts:215:5 › auth › client-side validation blocks submit on a too-short password (490ms)
    ok  9 [chromium] › tests\e2e\auth.spec.ts:233:3 › auth › native validation never pre-empts our copy (455ms)
    ok 10 [chromium] › tests\e2e\auth.spec.ts:249:3 › auth › a duplicate email is reported as such (3.4s)
    ok 11 [chromium] › tests\e2e\auth.spec.ts:260:3 › auth › the session cookie is httpOnly, SameSite=Lax and capped at 30 days (1.8s)
    ok 12 [chromium] › tests\e2e\auth.spec.ts:304:3 › auth › account deletion removes the user and returns to /login with the toast (13.7s)
    ok 13 [chromium] › tests\e2e\career.spec.ts:228:3 › career base › every career endpoint refuses a signed-out caller with 401 (1.0s)
    ok 14 [chromium] › tests\e2e\career.spec.ts:254:3 › career base › a new account sees the exact empty state (2.1s)
    ok 15 [chromium] › tests\e2e\career.spec.ts:260:3 › career base › a scanned PDF shows the exact 422 copy, pre-opens the paste tab, and saves nothing (3.3s)
    ok 16 [chromium] › tests\e2e\career.spec.ts:296:3 › career base › a text-layer PDF imports, reviews and saves, and a second import ADDS (9.5s)
    ok 17 [chromium] › tests\e2e\career.spec.ts:329:3 › career base › re-importing the SAME text saves nothing and reports every skip (8.8s)
    ok 18 [chromium] › tests\e2e\career.spec.ts:354:3 › career base › a metered Save spends once even when double-clicked (9.0s)
    ok 19 [chromium] › tests\e2e\generate.spec.ts:159:3 › generate › all four Phase-4 endpoints refuse a signed-out caller with 401 (3.1s)
    ok 20 [chromium] › tests\e2e\generate.spec.ts:181:3 › generate › another user's application is 404 on every Phase-4 endpoint, never 403 (2.1s)
    ok 21 [chromium] › tests\e2e\generate.spec.ts:197:3 › generate › generate, judge, edit, re-score, export — the whole US-4/US-5 path (40.9s)
    ok 22 [chromium] › tests\e2e\generate.spec.ts:522:3 › generate › one click, one spend: a double-clicked [Generate] sends ONE request (36.4s)
    ok 23 [chromium] › tests\e2e\generate.spec.ts:568:3 › generate › a saved display name becomes the resume name line and the file name (39.1s)
    ok 24 [chromium] › tests\e2e\generate.spec.ts:648:3 › generate › an empty editor blocks both metered buttons with the exact copy (12.4s)
    ok 25 [chromium] › tests\e2e\generate.spec.ts:672:3 › generate › the judge catches invention, and rule B2 forces revise however good the rest is (23.8s)
    ok 26 [chromium] › tests\e2e\generate.spec.ts:757:3 › generate › [Add to resume] is refused with a reason while no resume exists (12.8s)
    ok 27 [chromium] › tests\e2e\scan.spec.ts:214:3 › scan › both scan endpoints refuse a signed-out caller with 401 (847ms)
    ok 28 [chromium] › tests\e2e\scan.spec.ts:244:3 › scan › an oversized vacancy is refused with the exact copy, before any spend (1.7s)
    ok 29 [chromium] › tests\e2e\scan.spec.ts:275:3 › scan › an empty career base blocks the Career-base source with the exact notice (2.0s)
    ok 30 [chromium] › tests\e2e\scan.spec.ts:288:3 › scan › the applications list starts empty with the exact copy (2.0s)
    ok 31 [chromium] › tests\e2e\scan.spec.ts:294:3 › scan › a pasted resume scans, scores, and renders coverage, keywords and base matches (12.9s)
    ok 32 [chromium] › tests\e2e\scan.spec.ts:461:3 › scan › a career-base scan says the base is the source, instead of an empty tab (11.0s)
    -  33 [chromium] › tests\e2e\scan.spec.ts:503:3 › scan › an AI failure saves the vacancy as a draft and says so
    1 skipped
    32 passed (4.6m)

