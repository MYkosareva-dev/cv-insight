import { Placeholder } from '@/components/placeholder';

export const metadata = { title: 'New scan — CV Insight' };

export default function ScanPage() {
  return (
    <Placeholder
      title="New scan"
      description="Pick a resume source, paste a job posting, and run the analysis: parse → match → score."
      phase="the scan phase"
    />
  );
}
