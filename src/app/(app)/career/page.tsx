import { Placeholder } from '@/components/placeholder';

export const metadata = { title: 'Career base — CV Insight' };

export default function CareerPage() {
  return (
    <Placeholder
      title="Career base"
      description="Every role, project and achievement as an atomic career item — the only source of facts for a generated resume."
      phase="the career-base phase"
    />
  );
}
