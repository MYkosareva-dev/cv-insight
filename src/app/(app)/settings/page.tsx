import { Placeholder } from '@/components/placeholder';

export const metadata = { title: 'Settings — CV Insight' };

export default function SettingsPage() {
  return (
    <Placeholder
      title="Settings"
      description="Your email, sign out, and the danger zone: delete your account and all data."
      phase="the account phase"
    />
  );
}
