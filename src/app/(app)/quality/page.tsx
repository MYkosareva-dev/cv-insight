import { Placeholder } from '@/components/placeholder';

export const metadata = { title: 'Quality — CV Insight' };

export default function QualityPage() {
  return (
    <Placeholder
      title="Quality"
      description="Every OpenRouter call: step, model that actually served, fallback flag, tokens, cost and latency."
      phase="the observability phase"
    />
  );
}
